<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add Internet Permission for Email Service -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordClash">
        <activity
            android:name=".screens.ChangeDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".screens.ResetPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".screens.VerifyCodeActivity"
            android:exported="false" />
        <activity
            android:name=".screens.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".screens.UserListActivity"
            android:exported="false" />
        <activity
            android:name=".screens.admin_users_Activity"
            android:exported="false" />
        <!-- MAIN screen (first screen that opens) -->
        <activity
            android:name=".screens.StartPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Other screens -->
        <activity
            android:name=".screens.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".screens.MainActivity"
            android:exported="false" />
        <activity
            android:name=".screens.LoginActivity"
            android:exported="false" />
    </application>

</manifest>