<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WordClash" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.WordClash" parent="Base.Theme.WordClash" />

    <!-- Splash theme: shows gradient background instead of white screen -->
    <style name="Theme.WordClash.Splash" parent="Theme.WordClash">
        <item name="android:windowBackground">@drawable/gradient_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>